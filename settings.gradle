pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // Это решает твою ошибку
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "MyYoutubeBypass"
include ':app'
